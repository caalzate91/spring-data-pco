apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-banca-pvc
  namespace: test
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi