apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx
  namespace: nginx-service-lab
spec:
  type: ClusterIP
  selector:
    app: nginx
    tier: webapp
  ports:
    - name: http
      port: 80
      targetPort: 80
